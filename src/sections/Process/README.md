# Process

The "How it works" section, carried over from the original site — three phases
(Define / Align / Optimise) on the cream ground with a hairline rule per step.

Static by design (the motion lives in the rest of the page). Props: `steps[]`
from config.
